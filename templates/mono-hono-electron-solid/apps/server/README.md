# @mono-hono-electron-solid/server

This package contains the Hono backend for the `mono-hono-electron-solid` workspace.

## Usage

Run from the workspace root:

```bash
pnpm dev
pnpm --filter @mono-hono-electron-solid/server dev
```

The server runs on [http://localhost:3000](http://localhost:3000) by default.