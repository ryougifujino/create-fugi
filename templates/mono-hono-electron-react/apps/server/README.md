# @mono-hono-electron-react/server

This package contains the Hono backend for the `mono-hono-electron-react` workspace.

## Usage

Run from the workspace root:

```bash
pnpm dev
pnpm --filter @mono-hono-electron-react/server dev
```

The server runs on [http://localhost:3000](http://localhost:3000) by default.