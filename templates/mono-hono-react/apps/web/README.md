# @mono-hono-react/web

This package contains the React frontend for the `mono-hono-react` workspace.

## Usage

Run from the workspace root:

```bash
pnpm dev
pnpm --filter @mono-hono-react/web dev
```

The Vite dev server runs on [http://localhost:5173](http://localhost:5173) by default.
